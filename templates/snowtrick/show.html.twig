{% extends 'base.html.twig' %}

{% block title %}Snowtrick{% endblock %}

{% block body %}
<div class="container snowtrick-show">

    <a href="{{ path('app_snowtrick_index') }}" class="btn btn-dark">Retourner sur la page d'accueil</a>

    <h1>Snowtrick</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ snowtrick.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ snowtrick.title }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ snowtrick.author }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ snowtrick.slug }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ snowtrick.content }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ snowtrick.creationDate ? snowtrick.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ModificationDate</th>
                <td>{{ snowtrick.modificationDate ? snowtrick.modificationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_snowtrick_edit', {'id': snowtrick.id}) }}" class="btn btn-success">Modifier</a>

    {{ include('snowtrick/_delete_form.html.twig') }}

    <div class="comment-bloc">










        {{ include('snowtrick/_form.html.twig', {'button_label': 'Créer la figure'}) }}

    </div>

</div>
{% endblock %}
