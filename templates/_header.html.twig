{% block navbar %}
{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <div class="user-banner">
        <p>Bienvenue, {{ app.user.nickname }}.</p>
    </div>
{% endif %}

<nav class="navbar navbar-expand-sm navbar-dark bg-dark ">
    <div class="container-fluid justify-content-center">
        <a class="navbar-brand m-0" href=""></a>
        <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse justify-content-center" style="width:90%" id="navbarColor02">
            <ul class="navbar-nav" style="text-align:center">
                <div class="header-p1">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_snowtrick_index') }}"><i class="bi bi-house pe-2"></i>Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href=""><i class="bi bi-envelope pe-2"></i>Contact</a></li>
                </div>
                <div class="header-p2">
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><i class="bi bi-person-circle pe-2"></i>{{app.user.nickname}}</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-left pe-2"></i>Déconnexion</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('app_account_settings') }}"><i class="bi bi-gear-fill pe-2"></i>Paramètres de compte</a>
                            </div>
                        </li>
                    {% else %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><i class="bi bi-person-circle pe-2"></i>Compte</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('app_login') }}"><i class="bi bi-box-arrow-in-right pe-2"></i>Se connecter</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path('app_register') }}"><i class="bi bi-person-plus-fill pe-2"></i>Créer un compte</a>
                            </div>
                        </li>
                    {% endif %}
                </div>
            </ul>
        </div>
    </div>
</nav>
{% endblock %}